# Formatting

Formatting helpers:

- `format-cpp.sh`: formats Speed-owned C/C++ files under `src/` and `tests/`.
- `check-format.sh`: verifies C/C++ formatting without modifying files.
- `format.sh`: runs C/C++ formatting and Prettier.

Generated IPC output and third-party code are intentionally excluded.
